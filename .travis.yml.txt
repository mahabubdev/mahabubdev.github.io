language: node_js
node_js:
  - 14

cache:
  yarn: true

install:
  - yarn install

env: 
  CI: false
  REACT_APP_EMAILJS_USER_ID: $REACT_APP_EMAILJS_USER_ID
  REACT_APP_EMAILJS_SERVICE_KEY: $REACT_APP_EMAILJS_SERVICE_KEY
  REACT_APP_EMAILJS_TEM_ID: $REACT_APP_EMAILJS_TEM_ID

script:
  # - yarn test
  - yarn build

deploy:
  provider: pages
  skip-cleanup: true
  keep-history: true
  target_branch: gh-pages
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: main